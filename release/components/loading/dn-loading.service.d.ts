import { IDnLoadingConfig } from './dn-loading.config';
export declare class DnLoadingConfigService {
    private config;
    loadingConfig: IDnLoadingConfig;
    constructor(config: IDnLoadingConfig);
}
